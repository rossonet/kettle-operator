# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: cronkettlejobs.kettle.rossonet.net
spec:
  group: kettle.rossonet.net
  names:
    kind: CronKettleJob
    plural: cronkettlejobs
    shortNames:
    - kcj
    singular: cronkettlejob
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              definedRepository:
                description: Kettle Repository managed by Kubernetes
                type: string
              dir:
                description: "The repository directory that contains the job, including\
                  \ the leading slash"
                type: string
              export:
                description: Exports all linked resources of the specified job. The
                  argument is the name of a ZIP file.
                type: string
              file:
                description: "If you are calling a local KJB file, this is the filename,\
                  \ including the path if it is not in the local directory"
                type: string
              image:
                description: the Docker image default is rossonet/kettle-runner:latest
                type: string
              job:
                description: The name of the job (as it appears in the repository)
                  to launch
                type: string
              level:
                description: "The logging level (Basic, Detailed, Debug, Rowlevel,\
                  \ Error, Nothing)"
                type: string
              listdir:
                description: Lists the sub-directories within the specified repository
                  directory
                type: string
              listjob:
                description: Lists the jobs in the specified repository directory
                type: string
              listrep:
                description: Lists the available repositories
                type: string
              logfile:
                description: A local filename to write log output to
                type: string
              norep:
                description: "Prevents Kitchen from logging into a repository. If\
                  \ you have set the KETTLE_REPOSITORY, KETTLE_USER, and KETTLE_PASSWORD\
                  \ environment variables, then this option will enable you to prevent\
                  \ Kitchen from logging into the specified repository, assuming you\
                  \ would like to execute a local KTR file instead."
                type: string
              param:
                description: Set a named parameter in a name=value format. For
                type: string
              pass:
                description: Repository password
                type: string
              rep:
                description: "Enterprise or database repository name, if you are using\
                  \ one"
                type: string
              schedule:
                description: schedule notation like crontab
                type: string
              user:
                description: Repository username
                type: string
              version:
                description: "Shows the version, revision, and build date"
                type: string
              xmlRepository:
                description: XML with repositories configuration
                type: string
            required:
            - dir
            - schedule
            type: object
          status:
            properties:
              messages:
                items:
                  type: string
                type: array
              returnCode:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: cronkettletransformations.kettle.rossonet.net
spec:
  group: kettle.rossonet.net
  names:
    kind: CronKettleTransformation
    plural: cronkettletransformations
    shortNames:
    - kct
    singular: cronkettletransformation
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              definedRepository:
                description: Kettle Repository managed by Kubernetes
                type: string
              dir:
                description: "The repository directory that contains the transformation,\
                  \ including the leading slash"
                type: string
              exprep:
                description: Exports all repository objects to one XML file
                type: string
              file:
                description: "If you are calling a local KTR file, this is the filename,\
                  \ including the path if it is not in the local directory"
                type: string
              image:
                description: the Docker image default is rossonet/kettle-runner:latest
                type: string
              level:
                description: "The logging level (Basic, Detailed, Debug, Rowlevel,\
                  \ Error, Nothing)"
                type: string
              listdir:
                description: Lists the directories in the specified repository
                type: string
              listrep:
                description: Lists the available repositories
                type: string
              listtrans:
                description: Lists the transformations in the specified repository
                  directory
                type: string
              logfile:
                description: A local filename to write log output to
                type: string
              norep:
                description: "Prevents Pan from logging into a repository. If you\
                  \ have set the KETTLE_REPOSITORY, KETTLE_USER, and KETTLE_PASSWORD\
                  \ environment variables, then this option will enable you to prevent\
                  \ Pan from logging into the specified repository, assuming you would\
                  \ like to execute a local KTR file instead."
                type: string
              param:
                description: "Set a named parameter in a name=value format. For example:\
                  \ -param:FOO=bar"
                items:
                  type: string
                type: array
              pass:
                description: Repository password
                type: string
              rep:
                description: "Enterprise or database repository name, if you are using\
                  \ one"
                type: string
              safemode:
                description: "Runs in safe mode, which enables extra checking"
                type: string
              schedule:
                description: schedule notation like crontab
                type: string
              trans:
                description: The name of the transformation (as it appears in the
                  repository) to launch
                type: string
              user:
                description: Repository username
                type: string
              version:
                description: "Shows the version, revision, and build date"
                type: string
              xmlRepository:
                description: XML with repositories configuration
                type: string
            required:
            - dir
            - schedule
            type: object
          status:
            properties:
              messages:
                items:
                  type: string
                type: array
              returnCode:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: kettleides.kettle.rossonet.net
spec:
  group: kettle.rossonet.net
  names:
    kind: KettleIde
    plural: kettleides
    shortNames:
    - ki
    singular: kettleide
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              host:
                description: Ingress hostname
                type: string
              image:
                description: Docker image
                type: string
              path:
                description: Ingress path
                type: string
              repositories:
                description: Repositories managed by this ide
                items:
                  type: string
                type: array
            type: object
          status:
            properties:
              messages:
                items:
                  type: string
                type: array
              returnCode:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: kettlejobs.kettle.rossonet.net
spec:
  group: kettle.rossonet.net
  names:
    kind: KettleJob
    plural: kettlejobs
    shortNames:
    - kj
    singular: kettlejob
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              definedRepository:
                description: Kettle Repository managed by Kubernetes
                type: string
              dir:
                description: "The repository directory that contains the job, including\
                  \ the leading slash"
                type: string
              export:
                description: Exports all linked resources of the specified job. The
                  argument is the name of a ZIP file.
                type: string
              file:
                description: "If you are calling a local KJB file, this is the filename,\
                  \ including the path if it is not in the local directory"
                type: string
              image:
                description: the Docker image default is rossonet/kettle-runner:latest
                type: string
              job:
                description: The name of the job (as it appears in the repository)
                  to launch
                type: string
              level:
                description: "The logging level (Basic, Detailed, Debug, Rowlevel,\
                  \ Error, Nothing)"
                type: string
              listdir:
                description: Lists the sub-directories within the specified repository
                  directory
                type: string
              listjob:
                description: Lists the jobs in the specified repository directory
                type: string
              listrep:
                description: Lists the available repositories
                type: string
              logfile:
                description: A local filename to write log output to
                type: string
              norep:
                description: "Prevents Kitchen from logging into a repository. If\
                  \ you have set the KETTLE_REPOSITORY, KETTLE_USER, and KETTLE_PASSWORD\
                  \ environment variables, then this option will enable you to prevent\
                  \ Kitchen from logging into the specified repository, assuming you\
                  \ would like to execute a local KTR file instead."
                type: string
              param:
                description: Set a named parameter in a name=value format. For
                type: string
              pass:
                description: Repository password
                type: string
              rep:
                description: "Enterprise or database repository name, if you are using\
                  \ one"
                type: string
              user:
                description: Repository username
                type: string
              version:
                description: "Shows the version, revision, and build date"
                type: string
              xmlRepository:
                description: XML with repositories configuration
                type: string
            required:
            - dir
            type: object
          status:
            properties:
              messages:
                items:
                  type: string
                type: array
              returnCode:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: kettlerepositories.kettle.rossonet.net
spec:
  group: kettle.rossonet.net
  names:
    kind: KettleRepository
    plural: kettlerepositories
    shortNames:
    - kr
    singular: kettlerepository
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              databaseName:
                description: Database name
                type: string
              image:
                description: the Docker image default is rossonet/kettle-repository:latest
                type: string
              password:
                description: Database password
                type: string
              replicas:
                description: Database replicas
                type: integer
              repositoryPassword:
                description: Repository password
                type: string
              repositorySshCertificate:
                description: Repository ssh certificate
                type: string
              repositoryUrl:
                description: "repository url to get the data. Should start with the\
                  \ protocol 'git-ssh://', 'git-http://', 'git-https://', 's3://',\
                  \ 'ftp://', 'http://', 'https://' or 'file://'"
                type: string
              repositoryUsername:
                description: Repository username
                type: string
              username:
                description: Database username
                type: string
            type: object
          status:
            properties:
              messages:
                items:
                  type: string
                type: array
              returnCode:
                type: string
              totalJobs:
                type: integer
              totalTransformations:
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: kettletransformations.kettle.rossonet.net
spec:
  group: kettle.rossonet.net
  names:
    kind: KettleTransformation
    plural: kettletransformations
    shortNames:
    - kt
    singular: kettletransformation
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              dir:
                description: "The repository directory that contains the transformation,\
                  \ including the leading slash"
                type: string
              exprep:
                description: Exports all repository objects to one XML file
                type: string
              file:
                description: "If you are calling a local KTR file, this is the filename,\
                  \ including the path if it is not in the local directory"
                type: string
              image:
                description: the Docker image default is rossonet/kettle-runner:latest
                type: string
              level:
                description: "The logging level (Basic, Detailed, Debug, Rowlevel,\
                  \ Error, Nothing)"
                type: string
              listdir:
                description: Lists the directories in the specified repository
                type: string
              listrep:
                description: Lists the available repositories
                type: string
              listtrans:
                description: Lists the transformations in the specified repository
                  directory
                type: string
              logfile:
                description: A local filename to write log output to
                type: string
              norep:
                description: "Prevents Pan from logging into a repository. If you\
                  \ have set the KETTLE_REPOSITORY, KETTLE_USER, and KETTLE_PASSWORD\
                  \ environment variables, then this option will enable you to prevent\
                  \ Pan from logging into the specified repository, assuming you would\
                  \ like to execute a local KTR file instead."
                type: string
              param:
                description: "Set a named parameter in a name=value format. For example:\
                  \ -param:FOO=bar"
                items:
                  type: string
                type: array
              pass:
                description: Repository password
                type: string
              rep:
                description: "Enterprise or database repository name, if you are using\
                  \ one"
                type: string
              safemode:
                description: "Runs in safe mode, which enables extra checking"
                type: string
              trans:
                description: The name of the transformation (as it appears in the
                  repository) to launch
                type: string
              user:
                description: Repository username
                type: string
              version:
                description: "Shows the version, revision, and build date"
                type: string
              xmlRepository:
                description: XML with repositories configuration
                type: string
            required:
            - dir
            type: object
          status:
            properties:
              messages:
                items:
                  type: string
                type: array
              returnCode:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: cronkettlejobs.kettle.rossonet.net
spec:
  group: kettle.rossonet.net
  names:
    kind: CronKettleJob
    plural: cronkettlejobs
    shortNames:
    - kcj
    singular: cronkettlejob
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              definedRepository:
                description: Kettle Repository managed by Kubernetes
                type: string
              dir:
                description: "The repository directory that contains the job, including\
                  \ the leading slash"
                type: string
              export:
                description: Exports all linked resources of the specified job. The
                  argument is the name of a ZIP file.
                type: string
              file:
                description: "If you are calling a local KJB file, this is the filename,\
                  \ including the path if it is not in the local directory"
                type: string
              image:
                description: the Docker image default is rossonet/kettle-runner:latest
                type: string
              job:
                description: The name of the job (as it appears in the repository)
                  to launch
                type: string
              level:
                description: "The logging level (Basic, Detailed, Debug, Rowlevel,\
                  \ Error, Nothing)"
                type: string
              listdir:
                description: Lists the sub-directories within the specified repository
                  directory
                type: string
              listjob:
                description: Lists the jobs in the specified repository directory
                type: string
              listrep:
                description: Lists the available repositories
                type: string
              logfile:
                description: A local filename to write log output to
                type: string
              norep:
                description: "Prevents Kitchen from logging into a repository. If\
                  \ you have set the KETTLE_REPOSITORY, KETTLE_USER, and KETTLE_PASSWORD\
                  \ environment variables, then this option will enable you to prevent\
                  \ Kitchen from logging into the specified repository, assuming you\
                  \ would like to execute a local KTR file instead."
                type: string
              param:
                description: Set a named parameter in a name=value format. For
                items:
                  type: string
                type: array
              pass:
                description: Repository password
                type: string
              rep:
                description: "Enterprise or database repository name, if you are using\
                  \ one"
                type: string
              schedule:
                description: schedule notation like crontab
                type: string
              user:
                description: Repository username
                type: string
              version:
                description: "Shows the version, revision, and build date"
                type: string
              xmlRepository:
                description: XML with repositories configuration
                type: string
            required:
            - dir
            - schedule
            type: object
          status:
            properties:
              messages:
                items:
                  type: string
                type: array
              returnCode:
                type: integer
              returnCodeDescription:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: cronkettletransformations.kettle.rossonet.net
spec:
  group: kettle.rossonet.net
  names:
    kind: CronKettleTransformation
    plural: cronkettletransformations
    shortNames:
    - kct
    singular: cronkettletransformation
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              definedRepository:
                description: Kettle Repository managed by Kubernetes
                type: string
              dir:
                description: "The repository directory that contains the transformation,\
                  \ including the leading slash"
                type: string
              exprep:
                description: Exports all repository objects to one XML file
                type: string
              file:
                description: "If you are calling a local KTR file, this is the filename,\
                  \ including the path if it is not in the local directory"
                type: string
              image:
                description: the Docker image default is rossonet/kettle-runner:latest
                type: string
              level:
                description: "The logging level (Basic, Detailed, Debug, Rowlevel,\
                  \ Error, Nothing)"
                type: string
              listdir:
                description: Lists the directories in the specified repository
                type: string
              listrep:
                description: Lists the available repositories
                type: string
              listtrans:
                description: Lists the transformations in the specified repository
                  directory
                type: string
              logfile:
                description: A local filename to write log output to
                type: string
              norep:
                description: "Prevents Pan from logging into a repository. If you\
                  \ have set the KETTLE_REPOSITORY, KETTLE_USER, and KETTLE_PASSWORD\
                  \ environment variables, then this option will enable you to prevent\
                  \ Pan from logging into the specified repository, assuming you would\
                  \ like to execute a local KTR file instead."
                type: string
              param:
                description: "Set a named parameter in a name=value format. For example:\
                  \ -param:FOO=bar"
                items:
                  type: string
                type: array
              pass:
                description: Repository password
                type: string
              rep:
                description: "Enterprise or database repository name, if you are using\
                  \ one"
                type: string
              safemode:
                description: "Runs in safe mode, which enables extra checking"
                type: string
              schedule:
                description: schedule notation like crontab
                type: string
              trans:
                description: The name of the transformation (as it appears in the
                  repository) to launch
                type: string
              user:
                description: Repository username
                type: string
              version:
                description: "Shows the version, revision, and build date"
                type: string
              xmlRepository:
                description: XML with repositories configuration
                type: string
            required:
            - dir
            - schedule
            type: object
          status:
            properties:
              messages:
                items:
                  type: string
                type: array
              returnCode:
                type: integer
              returnCodeDescription:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: kettleides.kettle.rossonet.net
spec:
  group: kettle.rossonet.net
  names:
    kind: KettleIde
    plural: kettleides
    shortNames:
    - ki
    singular: kettleide
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              host:
                description: Ingress hostname
                type: string
              image:
                description: Docker image
                type: string
              path:
                description: Ingress path
                type: string
              repositories:
                description: Repositories managed by this ide
                items:
                  type: string
                type: array
            type: object
          status:
            properties:
              messages:
                items:
                  type: string
                type: array
              returnCode:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: kettlejobs.kettle.rossonet.net
spec:
  group: kettle.rossonet.net
  names:
    kind: KettleJob
    plural: kettlejobs
    shortNames:
    - kj
    singular: kettlejob
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              definedRepository:
                description: Kettle Repository managed by Kubernetes
                type: string
              dir:
                description: "The repository directory that contains the job, including\
                  \ the leading slash"
                type: string
              export:
                description: Exports all linked resources of the specified job. The
                  argument is the name of a ZIP file.
                type: string
              file:
                description: "If you are calling a local KJB file, this is the filename,\
                  \ including the path if it is not in the local directory"
                type: string
              image:
                description: the Docker image default is rossonet/kettle-runner:latest
                type: string
              job:
                description: The name of the job (as it appears in the repository)
                  to launch
                type: string
              level:
                description: "The logging level (Basic, Detailed, Debug, Rowlevel,\
                  \ Error, Nothing)"
                type: string
              listdir:
                description: Lists the sub-directories within the specified repository
                  directory
                type: string
              listjob:
                description: Lists the jobs in the specified repository directory
                type: string
              listrep:
                description: Lists the available repositories
                type: string
              logfile:
                description: A local filename to write log output to
                type: string
              norep:
                description: "Prevents Kitchen from logging into a repository. If\
                  \ you have set the KETTLE_REPOSITORY, KETTLE_USER, and KETTLE_PASSWORD\
                  \ environment variables, then this option will enable you to prevent\
                  \ Kitchen from logging into the specified repository, assuming you\
                  \ would like to execute a local KTR file instead."
                type: string
              param:
                description: Set a named parameter in a name=value format. For
                items:
                  type: string
                type: array
              pass:
                description: Repository password
                type: string
              rep:
                description: "Enterprise or database repository name, if you are using\
                  \ one"
                type: string
              user:
                description: Repository username
                type: string
              version:
                description: "Shows the version, revision, and build date"
                type: string
              xmlRepository:
                description: XML with repositories configuration
                type: string
            required:
            - dir
            type: object
          status:
            properties:
              messages:
                items:
                  type: string
                type: array
              returnCode:
                type: integer
              returnCodeDescription:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: kettlerepositories.kettle.rossonet.net
spec:
  group: kettle.rossonet.net
  names:
    kind: KettleRepository
    plural: kettlerepositories
    shortNames:
    - kr
    singular: kettlerepository
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              databaseName:
                description: Database name
                type: string
              image:
                description: the Docker image default is rossonet/kettle-repository:latest
                type: string
              password:
                description: Database password
                type: string
              replicas:
                description: Database replicas
                type: integer
              repositoryPassword:
                description: Repository password
                type: string
              repositorySshCertificate:
                description: Repository ssh certificate
                type: string
              repositoryUrl:
                description: "repository url to get the data. Should start with the\
                  \ protocol 'git-ssh://', 'git-http://', 'git-https://', 's3://',\
                  \ 'ftp://', 'http://', 'https://' or 'file://'"
                type: string
              repositoryUsername:
                description: Repository username
                type: string
              username:
                description: Database username
                type: string
            type: object
          status:
            properties:
              messages:
                items:
                  type: string
                type: array
              returnCode:
                type: string
              totalJobs:
                type: integer
              totalTransformations:
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: kettletransformations.kettle.rossonet.net
spec:
  group: kettle.rossonet.net
  names:
    kind: KettleTransformation
    plural: kettletransformations
    shortNames:
    - kt
    singular: kettletransformation
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              dir:
                description: "The repository directory that contains the transformation,\
                  \ including the leading slash"
                type: string
              exprep:
                description: Exports all repository objects to one XML file
                type: string
              file:
                description: "If you are calling a local KTR file, this is the filename,\
                  \ including the path if it is not in the local directory"
                type: string
              image:
                description: the Docker image default is rossonet/kettle-runner:latest
                type: string
              level:
                description: "The logging level (Basic, Detailed, Debug, Rowlevel,\
                  \ Error, Nothing)"
                type: string
              listdir:
                description: Lists the directories in the specified repository
                type: string
              listrep:
                description: Lists the available repositories
                type: string
              listtrans:
                description: Lists the transformations in the specified repository
                  directory
                type: string
              logfile:
                description: A local filename to write log output to
                type: string
              norep:
                description: "Prevents Pan from logging into a repository. If you\
                  \ have set the KETTLE_REPOSITORY, KETTLE_USER, and KETTLE_PASSWORD\
                  \ environment variables, then this option will enable you to prevent\
                  \ Pan from logging into the specified repository, assuming you would\
                  \ like to execute a local KTR file instead."
                type: string
              param:
                description: "Set a named parameter in a name=value format. For example:\
                  \ -param:FOO=bar"
                items:
                  type: string
                type: array
              pass:
                description: Repository password
                type: string
              rep:
                description: "Enterprise or database repository name, if you are using\
                  \ one"
                type: string
              safemode:
                description: "Runs in safe mode, which enables extra checking"
                type: string
              trans:
                description: The name of the transformation (as it appears in the
                  repository) to launch
                type: string
              user:
                description: Repository username
                type: string
              version:
                description: "Shows the version, revision, and build date"
                type: string
              xmlRepository:
                description: XML with repositories configuration
                type: string
            required:
            - dir
            type: object
          status:
            properties:
              messages:
                items:
                  type: string
                type: array
              returnCode:
                type: integer
              returnCodeDescription:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: cronkettlejobs.kettle.rossonet.net
spec:
  group: kettle.rossonet.net
  names:
    kind: CronKettleJob
    plural: cronkettlejobs
    shortNames:
    - kcj
    singular: cronkettlejob
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              dir:
                description: "The repository directory that contains the job, including\
                  \ the leading slash"
                type: string
              export:
                description: Exports all linked resources of the specified job. The
                  argument is the name of a ZIP file.
                type: string
              file:
                description: "If you are calling a local KJB file, this is the filename,\
                  \ including the path if it is not in the local directory"
                type: string
              image:
                description: the Docker image default is rossonet/kettle-runner:latest
                type: string
              job:
                description: The name of the job (as it appears in the repository)
                  to launch
                type: string
              level:
                description: "The logging level (Basic, Detailed, Debug, Rowlevel,\
                  \ Error, Nothing)"
                type: string
              listdir:
                description: Lists the sub-directories within the specified repository
                  directory
                type: string
              listjob:
                description: Lists the jobs in the specified repository directory
                type: string
              listrep:
                description: Lists the available repositories
                type: string
              logfile:
                description: A local filename to write log output to
                type: string
              norep:
                description: "Prevents Kitchen from logging into a repository. If\
                  \ you have set the KETTLE_REPOSITORY, KETTLE_USER, and KETTLE_PASSWORD\
                  \ environment variables, then this option will enable you to prevent\
                  \ Kitchen from logging into the specified repository, assuming you\
                  \ would like to execute a local KTR file instead."
                type: string
              param:
                description: Set a named parameter in a name=value format. For
                items:
                  type: string
                type: array
              pass:
                description: Repository password
                type: string
              rep:
                description: "Enterprise or database repository name, if you are using\
                  \ one"
                type: string
              schedule:
                description: schedule notation like crontab
                type: string
              user:
                description: Repository username
                type: string
              version:
                description: "Shows the version, revision, and build date"
                type: string
              xmlRepository:
                description: XML with repositories configuration
                type: string
            required:
            - dir
            - schedule
            type: object
          status:
            properties:
              messages:
                items:
                  type: string
                type: array
              returnCode:
                type: integer
              returnCodeDescription:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: cronkettletransformations.kettle.rossonet.net
spec:
  group: kettle.rossonet.net
  names:
    kind: CronKettleTransformation
    plural: cronkettletransformations
    shortNames:
    - kct
    singular: cronkettletransformation
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              dir:
                description: "The repository directory that contains the transformation,\
                  \ including the leading slash"
                type: string
              exprep:
                description: Exports all repository objects to one XML file
                type: string
              file:
                description: "If you are calling a local KTR file, this is the filename,\
                  \ including the path if it is not in the local directory"
                type: string
              image:
                description: the Docker image default is rossonet/kettle-runner:latest
                type: string
              level:
                description: "The logging level (Basic, Detailed, Debug, Rowlevel,\
                  \ Error, Nothing)"
                type: string
              listdir:
                description: Lists the directories in the specified repository
                type: string
              listrep:
                description: Lists the available repositories
                type: string
              listtrans:
                description: Lists the transformations in the specified repository
                  directory
                type: string
              logfile:
                description: A local filename to write log output to
                type: string
              norep:
                description: "Prevents Pan from logging into a repository. If you\
                  \ have set the KETTLE_REPOSITORY, KETTLE_USER, and KETTLE_PASSWORD\
                  \ environment variables, then this option will enable you to prevent\
                  \ Pan from logging into the specified repository, assuming you would\
                  \ like to execute a local KTR file instead."
                type: string
              param:
                description: "Set a named parameter in a name=value format. For example:\
                  \ -param:FOO=bar"
                items:
                  type: string
                type: array
              pass:
                description: Repository password
                type: string
              rep:
                description: "Enterprise or database repository name, if you are using\
                  \ one"
                type: string
              safemode:
                description: "Runs in safe mode, which enables extra checking"
                type: string
              schedule:
                description: schedule notation like crontab
                type: string
              trans:
                description: The name of the transformation (as it appears in the
                  repository) to launch
                type: string
              user:
                description: Repository username
                type: string
              version:
                description: "Shows the version, revision, and build date"
                type: string
              xmlRepository:
                description: XML with repositories configuration
                type: string
            required:
            - dir
            - schedule
            type: object
          status:
            properties:
              messages:
                items:
                  type: string
                type: array
              returnCode:
                type: integer
              returnCodeDescription:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: kettleides.kettle.rossonet.net
spec:
  group: kettle.rossonet.net
  names:
    kind: KettleIde
    plural: kettleides
    shortNames:
    - ki
    singular: kettleide
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              host:
                description: Ingress hostname
                type: string
              image:
                description: Docker image
                type: string
              path:
                description: Ingress path
                type: string
              repositories:
                description: Repositories managed by this ide
                items:
                  type: string
                type: array
              xmlRepository:
                description: XML with repositories configuration
                type: string
            type: object
          status:
            properties:
              messages:
                items:
                  type: string
                type: array
              returnCode:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: kettlejobs.kettle.rossonet.net
spec:
  group: kettle.rossonet.net
  names:
    kind: KettleJob
    plural: kettlejobs
    shortNames:
    - kj
    singular: kettlejob
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              dir:
                description: "The repository directory that contains the job, including\
                  \ the leading slash"
                type: string
              export:
                description: Exports all linked resources of the specified job. The
                  argument is the name of a ZIP file.
                type: string
              file:
                description: "If you are calling a local KJB file, this is the filename,\
                  \ including the path if it is not in the local directory"
                type: string
              image:
                description: the Docker image default is rossonet/kettle-runner:latest
                type: string
              job:
                description: The name of the job (as it appears in the repository)
                  to launch
                type: string
              level:
                description: "The logging level (Basic, Detailed, Debug, Rowlevel,\
                  \ Error, Nothing)"
                type: string
              listdir:
                description: Lists the sub-directories within the specified repository
                  directory
                type: string
              listjob:
                description: Lists the jobs in the specified repository directory
                type: string
              listrep:
                description: Lists the available repositories
                type: string
              logfile:
                description: A local filename to write log output to
                type: string
              norep:
                description: "Prevents Kitchen from logging into a repository. If\
                  \ you have set the KETTLE_REPOSITORY, KETTLE_USER, and KETTLE_PASSWORD\
                  \ environment variables, then this option will enable you to prevent\
                  \ Kitchen from logging into the specified repository, assuming you\
                  \ would like to execute a local KTR file instead."
                type: string
              param:
                description: Set a named parameter in a name=value format. For
                items:
                  type: string
                type: array
              pass:
                description: Repository password
                type: string
              rep:
                description: "Enterprise or database repository name, if you are using\
                  \ one"
                type: string
              user:
                description: Repository username
                type: string
              version:
                description: "Shows the version, revision, and build date"
                type: string
              xmlRepository:
                description: XML with repositories configuration
                type: string
            required:
            - dir
            type: object
          status:
            properties:
              messages:
                items:
                  type: string
                type: array
              returnCode:
                type: integer
              returnCodeDescription:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: kettlerepositories.kettle.rossonet.net
spec:
  group: kettle.rossonet.net
  names:
    kind: KettleRepository
    plural: kettlerepositories
    shortNames:
    - kr
    singular: kettlerepository
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              databaseName:
                description: Database name
                type: string
              image:
                description: the Docker image default is rossonet/kettle-repository:latest
                type: string
              password:
                description: Database password
                type: string
              replicas:
                description: Database replicas
                type: integer
              repositoryPassword:
                description: Repository password
                type: string
              repositorySshCertificate:
                description: Repository ssh certificate
                type: string
              repositoryUrl:
                description: "repository url to get the data. Should start with the\
                  \ protocol 'git-ssh://', 'git-http://', 'git-https://', 's3://',\
                  \ 'ftp://', 'http://', 'https://' or 'file://'"
                type: string
              repositoryUsername:
                description: Repository username
                type: string
              username:
                description: Database username
                type: string
            type: object
          status:
            properties:
              messages:
                items:
                  type: string
                type: array
              returnCode:
                type: string
              totalJobs:
                type: integer
              totalTransformations:
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: kettletransformations.kettle.rossonet.net
spec:
  group: kettle.rossonet.net
  names:
    kind: KettleTransformation
    plural: kettletransformations
    shortNames:
    - kt
    singular: kettletransformation
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              dir:
                description: "The repository directory that contains the transformation,\
                  \ including the leading slash"
                type: string
              exprep:
                description: Exports all repository objects to one XML file
                type: string
              file:
                description: "If you are calling a local KTR file, this is the filename,\
                  \ including the path if it is not in the local directory"
                type: string
              image:
                description: the Docker image default is rossonet/kettle-runner:latest
                type: string
              level:
                description: "The logging level (Basic, Detailed, Debug, Rowlevel,\
                  \ Error, Nothing)"
                type: string
              listdir:
                description: Lists the directories in the specified repository
                type: string
              listrep:
                description: Lists the available repositories
                type: string
              listtrans:
                description: Lists the transformations in the specified repository
                  directory
                type: string
              logfile:
                description: A local filename to write log output to
                type: string
              norep:
                description: "Prevents Pan from logging into a repository. If you\
                  \ have set the KETTLE_REPOSITORY, KETTLE_USER, and KETTLE_PASSWORD\
                  \ environment variables, then this option will enable you to prevent\
                  \ Pan from logging into the specified repository, assuming you would\
                  \ like to execute a local KTR file instead."
                type: string
              param:
                description: "Set a named parameter in a name=value format. For example:\
                  \ -param:FOO=bar"
                items:
                  type: string
                type: array
              pass:
                description: Repository password
                type: string
              rep:
                description: "Enterprise or database repository name, if you are using\
                  \ one"
                type: string
              safemode:
                description: "Runs in safe mode, which enables extra checking"
                type: string
              trans:
                description: The name of the transformation (as it appears in the
                  repository) to launch
                type: string
              user:
                description: Repository username
                type: string
              version:
                description: "Shows the version, revision, and build date"
                type: string
              xmlRepository:
                description: XML with repositories configuration
                type: string
            required:
            - dir
            type: object
          status:
            properties:
              messages:
                items:
                  type: string
                type: array
              returnCode:
                type: integer
              returnCodeDescription:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: cronkettlejobs.kettle.rossonet.net
spec:
  group: kettle.rossonet.net
  names:
    kind: CronKettleJob
    plural: cronkettlejobs
    shortNames:
    - kcj
    singular: cronkettlejob
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              dir:
                description: "The repository directory that contains the job, including\
                  \ the leading slash"
                type: string
              export:
                description: Exports all linked resources of the specified job. The
                  argument is the name of a ZIP file.
                type: string
              file:
                description: "If you are calling a local KJB file, this is the filename,\
                  \ including the path if it is not in the local directory"
                type: string
              image:
                description: the Docker image default is rossonet/kettle-runner:latest
                type: string
              job:
                description: The name of the job (as it appears in the repository)
                  to launch
                type: string
              level:
                description: "The logging level (Basic, Detailed, Debug, Rowlevel,\
                  \ Error, Nothing)"
                type: string
              listdir:
                description: Lists the sub-directories within the specified repository
                  directory
                type: string
              listjob:
                description: Lists the jobs in the specified repository directory
                type: string
              listrep:
                description: Lists the available repositories
                type: string
              logfile:
                description: A local filename to write log output to
                type: string
              norep:
                description: "Prevents Kitchen from logging into a repository. If\
                  \ you have set the KETTLE_REPOSITORY, KETTLE_USER, and KETTLE_PASSWORD\
                  \ environment variables, then this option will enable you to prevent\
                  \ Kitchen from logging into the specified repository, assuming you\
                  \ would like to execute a local KTR file instead."
                type: string
              param:
                description: Set a named parameter in a name=value format. For
                items:
                  type: string
                type: array
              pass:
                description: Repository password
                type: string
              rep:
                description: "Enterprise or database repository name, if you are using\
                  \ one"
                type: string
              schedule:
                description: schedule notation like crontab
                type: string
              user:
                description: Repository username
                type: string
              version:
                description: "Shows the version, revision, and build date"
                type: string
              xmlRepository:
                description: XML with repositories configuration
                type: string
            required:
            - dir
            - schedule
            type: object
          status:
            properties:
              messages:
                items:
                  type: string
                type: array
              returnCode:
                type: integer
              returnCodeDescription:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: cronkettletransformations.kettle.rossonet.net
spec:
  group: kettle.rossonet.net
  names:
    kind: CronKettleTransformation
    plural: cronkettletransformations
    shortNames:
    - kct
    singular: cronkettletransformation
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              dir:
                description: "The repository directory that contains the transformation,\
                  \ including the leading slash"
                type: string
              exprep:
                description: Exports all repository objects to one XML file
                type: string
              file:
                description: "If you are calling a local KTR file, this is the filename,\
                  \ including the path if it is not in the local directory"
                type: string
              image:
                description: the Docker image default is rossonet/kettle-runner:latest
                type: string
              level:
                description: "The logging level (Basic, Detailed, Debug, Rowlevel,\
                  \ Error, Nothing)"
                type: string
              listdir:
                description: Lists the directories in the specified repository
                type: string
              listrep:
                description: Lists the available repositories
                type: string
              listtrans:
                description: Lists the transformations in the specified repository
                  directory
                type: string
              logfile:
                description: A local filename to write log output to
                type: string
              norep:
                description: "Prevents Pan from logging into a repository. If you\
                  \ have set the KETTLE_REPOSITORY, KETTLE_USER, and KETTLE_PASSWORD\
                  \ environment variables, then this option will enable you to prevent\
                  \ Pan from logging into the specified repository, assuming you would\
                  \ like to execute a local KTR file instead."
                type: string
              param:
                description: "Set a named parameter in a name=value format. For example:\
                  \ -param:FOO=bar"
                items:
                  type: string
                type: array
              pass:
                description: Repository password
                type: string
              rep:
                description: "Enterprise or database repository name, if you are using\
                  \ one"
                type: string
              safemode:
                description: "Runs in safe mode, which enables extra checking"
                type: string
              schedule:
                description: schedule notation like crontab
                type: string
              trans:
                description: The name of the transformation (as it appears in the
                  repository) to launch
                type: string
              user:
                description: Repository username
                type: string
              version:
                description: "Shows the version, revision, and build date"
                type: string
              xmlRepository:
                description: XML with repositories configuration
                type: string
            required:
            - dir
            - schedule
            type: object
          status:
            properties:
              messages:
                items:
                  type: string
                type: array
              returnCode:
                type: integer
              returnCodeDescription:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: kettleides.kettle.rossonet.net
spec:
  group: kettle.rossonet.net
  names:
    kind: KettleIde
    plural: kettleides
    shortNames:
    - ki
    singular: kettleide
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              host:
                description: Ingress hostname
                type: string
              image:
                description: Docker image
                type: string
              path:
                description: Ingress path
                type: string
              repositories:
                description: Repositories managed by this ide
                items:
                  type: string
                type: array
              xmlRepository:
                description: XML with repositories configuration
                type: string
            type: object
          status:
            properties:
              messages:
                items:
                  type: string
                type: array
              returnCode:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: kettlejobs.kettle.rossonet.net
spec:
  group: kettle.rossonet.net
  names:
    kind: KettleJob
    plural: kettlejobs
    shortNames:
    - kj
    singular: kettlejob
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              dir:
                description: "The repository directory that contains the job, including\
                  \ the leading slash"
                type: string
              export:
                description: Exports all linked resources of the specified job. The
                  argument is the name of a ZIP file.
                type: string
              file:
                description: "If you are calling a local KJB file, this is the filename,\
                  \ including the path if it is not in the local directory"
                type: string
              image:
                description: the Docker image default is rossonet/kettle-runner:latest
                type: string
              job:
                description: The name of the job (as it appears in the repository)
                  to launch
                type: string
              level:
                description: "The logging level (Basic, Detailed, Debug, Rowlevel,\
                  \ Error, Nothing)"
                type: string
              listdir:
                description: Lists the sub-directories within the specified repository
                  directory
                type: string
              listjob:
                description: Lists the jobs in the specified repository directory
                type: string
              listrep:
                description: Lists the available repositories
                type: string
              logfile:
                description: A local filename to write log output to
                type: string
              norep:
                description: "Prevents Kitchen from logging into a repository. If\
                  \ you have set the KETTLE_REPOSITORY, KETTLE_USER, and KETTLE_PASSWORD\
                  \ environment variables, then this option will enable you to prevent\
                  \ Kitchen from logging into the specified repository, assuming you\
                  \ would like to execute a local KTR file instead."
                type: string
              param:
                description: Set a named parameter in a name=value format. For
                items:
                  type: string
                type: array
              pass:
                description: Repository password
                type: string
              rep:
                description: "Enterprise or database repository name, if you are using\
                  \ one"
                type: string
              user:
                description: Repository username
                type: string
              version:
                description: "Shows the version, revision, and build date"
                type: string
              xmlRepository:
                description: XML with repositories configuration
                type: string
            required:
            - dir
            type: object
          status:
            properties:
              messages:
                items:
                  type: string
                type: array
              returnCode:
                type: integer
              returnCodeDescription:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: kettlerepositories.kettle.rossonet.net
spec:
  group: kettle.rossonet.net
  names:
    kind: KettleRepository
    plural: kettlerepositories
    shortNames:
    - kr
    singular: kettlerepository
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              databaseName:
                description: Database name
                type: string
              image:
                description: the Docker image default is rossonet/kettle-repository:latest
                type: string
              password:
                description: Database password
                type: string
              replicas:
                description: Database replicas
                type: integer
              repositoryPassword:
                description: Repository password
                type: string
              repositorySshCertificate:
                description: Repository ssh certificate
                type: string
              repositoryUrl:
                description: "repository url to get the data. Should start with the\
                  \ protocol 'git-ssh://', 'git-http://', 'git-https://', 's3://',\
                  \ 'ftp://', 'http://', 'https://' or 'file://'"
                type: string
              repositoryUsername:
                description: Repository username
                type: string
              username:
                description: Database username
                type: string
            type: object
          status:
            properties:
              messages:
                items:
                  type: string
                type: array
              returnCode:
                type: string
              totalJobs:
                type: integer
              totalTransformations:
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: kettletransformations.kettle.rossonet.net
spec:
  group: kettle.rossonet.net
  names:
    kind: KettleTransformation
    plural: kettletransformations
    shortNames:
    - kt
    singular: kettletransformation
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              dir:
                description: "The repository directory that contains the transformation,\
                  \ including the leading slash"
                type: string
              exprep:
                description: Exports all repository objects to one XML file
                type: string
              file:
                description: "If you are calling a local KTR file, this is the filename,\
                  \ including the path if it is not in the local directory"
                type: string
              image:
                description: the Docker image default is rossonet/kettle-runner:latest
                type: string
              level:
                description: "The logging level (Basic, Detailed, Debug, Rowlevel,\
                  \ Error, Nothing)"
                type: string
              listdir:
                description: Lists the directories in the specified repository
                type: string
              listrep:
                description: Lists the available repositories
                type: string
              listtrans:
                description: Lists the transformations in the specified repository
                  directory
                type: string
              logfile:
                description: A local filename to write log output to
                type: string
              norep:
                description: "Prevents Pan from logging into a repository. If you\
                  \ have set the KETTLE_REPOSITORY, KETTLE_USER, and KETTLE_PASSWORD\
                  \ environment variables, then this option will enable you to prevent\
                  \ Pan from logging into the specified repository, assuming you would\
                  \ like to execute a local KTR file instead."
                type: string
              param:
                description: "Set a named parameter in a name=value format. For example:\
                  \ -param:FOO=bar"
                items:
                  type: string
                type: array
              pass:
                description: Repository password
                type: string
              rep:
                description: "Enterprise or database repository name, if you are using\
                  \ one"
                type: string
              safemode:
                description: "Runs in safe mode, which enables extra checking"
                type: string
              trans:
                description: The name of the transformation (as it appears in the
                  repository) to launch
                type: string
              user:
                description: Repository username
                type: string
              version:
                description: "Shows the version, revision, and build date"
                type: string
              xmlRepository:
                description: XML with repositories configuration
                type: string
            required:
            - dir
            type: object
          status:
            properties:
              messages:
                items:
                  type: string
                type: array
              returnCode:
                type: integer
              returnCodeDescription:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: cronkettlejobs.kettle.rossonet.net
spec:
  group: kettle.rossonet.net
  names:
    kind: CronKettleJob
    plural: cronkettlejobs
    shortNames:
    - kcj
    singular: cronkettlejob
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              dir:
                description: "The repository directory that contains the job, including\
                  \ the leading slash"
                type: string
              export:
                description: Exports all linked resources of the specified job. The
                  argument is the name of a ZIP file.
                type: string
              file:
                description: "If you are calling a local KJB file, this is the filename,\
                  \ including the path if it is not in the local directory"
                type: string
              image:
                description: the Docker image default is rossonet/kettle-runner:latest
                type: string
              job:
                description: The name of the job (as it appears in the repository)
                  to launch
                type: string
              level:
                description: "The logging level (Basic, Detailed, Debug, Rowlevel,\
                  \ Error, Nothing)"
                type: string
              listdir:
                description: Lists the sub-directories within the specified repository
                  directory
                type: string
              listjob:
                description: Lists the jobs in the specified repository directory
                type: string
              listrep:
                description: Lists the available repositories
                type: string
              logfile:
                description: A local filename to write log output to
                type: string
              norep:
                description: "Prevents Kitchen from logging into a repository. If\
                  \ you have set the KETTLE_REPOSITORY, KETTLE_USER, and KETTLE_PASSWORD\
                  \ environment variables, then this option will enable you to prevent\
                  \ Kitchen from logging into the specified repository, assuming you\
                  \ would like to execute a local KTR file instead."
                type: string
              param:
                description: Set a named parameter in a name=value format. For
                items:
                  type: string
                type: array
              pass:
                description: Repository password
                type: string
              rep:
                description: "Enterprise or database repository name, if you are using\
                  \ one"
                type: string
              schedule:
                description: schedule notation like crontab
                type: string
              user:
                description: Repository username
                type: string
              version:
                description: "Shows the version, revision, and build date"
                type: string
              xmlRepository:
                description: XML with repositories configuration
                type: string
            required:
            - dir
            - schedule
            type: object
          status:
            properties:
              messages:
                items:
                  type: string
                type: array
              returnCode:
                type: integer
              returnCodeDescription:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: cronkettletransformations.kettle.rossonet.net
spec:
  group: kettle.rossonet.net
  names:
    kind: CronKettleTransformation
    plural: cronkettletransformations
    shortNames:
    - kct
    singular: cronkettletransformation
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              dir:
                description: "The repository directory that contains the transformation,\
                  \ including the leading slash"
                type: string
              exprep:
                description: Exports all repository objects to one XML file
                type: string
              file:
                description: "If you are calling a local KTR file, this is the filename,\
                  \ including the path if it is not in the local directory"
                type: string
              image:
                description: the Docker image default is rossonet/kettle-runner:latest
                type: string
              level:
                description: "The logging level (Basic, Detailed, Debug, Rowlevel,\
                  \ Error, Nothing)"
                type: string
              listdir:
                description: Lists the directories in the specified repository
                type: string
              listrep:
                description: Lists the available repositories
                type: string
              listtrans:
                description: Lists the transformations in the specified repository
                  directory
                type: string
              logfile:
                description: A local filename to write log output to
                type: string
              norep:
                description: "Prevents Pan from logging into a repository. If you\
                  \ have set the KETTLE_REPOSITORY, KETTLE_USER, and KETTLE_PASSWORD\
                  \ environment variables, then this option will enable you to prevent\
                  \ Pan from logging into the specified repository, assuming you would\
                  \ like to execute a local KTR file instead."
                type: string
              param:
                description: "Set a named parameter in a name=value format. For example:\
                  \ -param:FOO=bar"
                items:
                  type: string
                type: array
              pass:
                description: Repository password
                type: string
              rep:
                description: "Enterprise or database repository name, if you are using\
                  \ one"
                type: string
              safemode:
                description: "Runs in safe mode, which enables extra checking"
                type: string
              schedule:
                description: schedule notation like crontab
                type: string
              trans:
                description: The name of the transformation (as it appears in the
                  repository) to launch
                type: string
              user:
                description: Repository username
                type: string
              version:
                description: "Shows the version, revision, and build date"
                type: string
              xmlRepository:
                description: XML with repositories configuration
                type: string
            required:
            - dir
            - schedule
            type: object
          status:
            properties:
              messages:
                items:
                  type: string
                type: array
              returnCode:
                type: integer
              returnCodeDescription:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: kettleides.kettle.rossonet.net
spec:
  group: kettle.rossonet.net
  names:
    kind: KettleIde
    plural: kettleides
    shortNames:
    - ki
    singular: kettleide
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              host:
                description: Ingress hostname
                type: string
              image:
                description: Docker image
                type: string
              path:
                description: Ingress path
                type: string
              xmlRepository:
                description: XML with repositories configuration
                type: string
            type: object
          status:
            properties:
              messages:
                items:
                  type: string
                type: array
              returnCode:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: kettlejobs.kettle.rossonet.net
spec:
  group: kettle.rossonet.net
  names:
    kind: KettleJob
    plural: kettlejobs
    shortNames:
    - kj
    singular: kettlejob
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              dir:
                description: "The repository directory that contains the job, including\
                  \ the leading slash"
                type: string
              export:
                description: Exports all linked resources of the specified job. The
                  argument is the name of a ZIP file.
                type: string
              file:
                description: "If you are calling a local KJB file, this is the filename,\
                  \ including the path if it is not in the local directory"
                type: string
              image:
                description: the Docker image default is rossonet/kettle-runner:latest
                type: string
              job:
                description: The name of the job (as it appears in the repository)
                  to launch
                type: string
              level:
                description: "The logging level (Basic, Detailed, Debug, Rowlevel,\
                  \ Error, Nothing)"
                type: string
              listdir:
                description: Lists the sub-directories within the specified repository
                  directory
                type: string
              listjob:
                description: Lists the jobs in the specified repository directory
                type: string
              listrep:
                description: Lists the available repositories
                type: string
              logfile:
                description: A local filename to write log output to
                type: string
              norep:
                description: "Prevents Kitchen from logging into a repository. If\
                  \ you have set the KETTLE_REPOSITORY, KETTLE_USER, and KETTLE_PASSWORD\
                  \ environment variables, then this option will enable you to prevent\
                  \ Kitchen from logging into the specified repository, assuming you\
                  \ would like to execute a local KTR file instead."
                type: string
              param:
                description: Set a named parameter in a name=value format. For
                items:
                  type: string
                type: array
              pass:
                description: Repository password
                type: string
              rep:
                description: "Enterprise or database repository name, if you are using\
                  \ one"
                type: string
              user:
                description: Repository username
                type: string
              version:
                description: "Shows the version, revision, and build date"
                type: string
              xmlRepository:
                description: XML with repositories configuration
                type: string
            required:
            - dir
            type: object
          status:
            properties:
              messages:
                items:
                  type: string
                type: array
              returnCode:
                type: integer
              returnCodeDescription:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: kettlerepositories.kettle.rossonet.net
spec:
  group: kettle.rossonet.net
  names:
    kind: KettleRepository
    plural: kettlerepositories
    shortNames:
    - kr
    singular: kettlerepository
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              databaseName:
                description: Database name
                type: string
              image:
                description: the Docker image default is rossonet/kettle-repository:latest
                type: string
              password:
                description: Database password
                type: string
              replicas:
                description: Database replicas
                type: integer
              repositoryPassword:
                description: Repository password
                type: string
              repositorySshCertificate:
                description: Repository ssh certificate
                type: string
              repositoryUrl:
                description: "repository url to get the data. Should start with the\
                  \ protocol 'git-ssh://', 'git-http://', 'git-https://', 's3://',\
                  \ 'ftp://', 'http://', 'https://' or 'file://'"
                type: string
              repositoryUsername:
                description: Repository username
                type: string
              username:
                description: Database username
                type: string
            type: object
          status:
            properties:
              messages:
                items:
                  type: string
                type: array
              returnCode:
                type: string
              totalJobs:
                type: integer
              totalTransformations:
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: kettletransformations.kettle.rossonet.net
spec:
  group: kettle.rossonet.net
  names:
    kind: KettleTransformation
    plural: kettletransformations
    shortNames:
    - kt
    singular: kettletransformation
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              dir:
                description: "The repository directory that contains the transformation,\
                  \ including the leading slash"
                type: string
              exprep:
                description: Exports all repository objects to one XML file
                type: string
              file:
                description: "If you are calling a local KTR file, this is the filename,\
                  \ including the path if it is not in the local directory"
                type: string
              image:
                description: the Docker image default is rossonet/kettle-runner:latest
                type: string
              level:
                description: "The logging level (Basic, Detailed, Debug, Rowlevel,\
                  \ Error, Nothing)"
                type: string
              listdir:
                description: Lists the directories in the specified repository
                type: string
              listrep:
                description: Lists the available repositories
                type: string
              listtrans:
                description: Lists the transformations in the specified repository
                  directory
                type: string
              logfile:
                description: A local filename to write log output to
                type: string
              norep:
                description: "Prevents Pan from logging into a repository. If you\
                  \ have set the KETTLE_REPOSITORY, KETTLE_USER, and KETTLE_PASSWORD\
                  \ environment variables, then this option will enable you to prevent\
                  \ Pan from logging into the specified repository, assuming you would\
                  \ like to execute a local KTR file instead."
                type: string
              param:
                description: "Set a named parameter in a name=value format. For example:\
                  \ -param:FOO=bar"
                items:
                  type: string
                type: array
              pass:
                description: Repository password
                type: string
              rep:
                description: "Enterprise or database repository name, if you are using\
                  \ one"
                type: string
              safemode:
                description: "Runs in safe mode, which enables extra checking"
                type: string
              trans:
                description: The name of the transformation (as it appears in the
                  repository) to launch
                type: string
              user:
                description: Repository username
                type: string
              version:
                description: "Shows the version, revision, and build date"
                type: string
              xmlRepository:
                description: XML with repositories configuration
                type: string
            type: object
          status:
            properties:
              messages:
                items:
                  type: string
                type: array
              returnCode:
                type: integer
              returnCodeDescription:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: cronkettlejobs.kettle.rossonet.net
spec:
  group: kettle.rossonet.net
  names:
    kind: CronKettleJob
    plural: cronkettlejobs
    shortNames:
    - kcj
    singular: cronkettlejob
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              dir:
                description: "The repository directory that contains the job, including\
                  \ the leading slash"
                type: string
              export:
                description: Exports all linked resources of the specified job. The
                  argument is the name of a ZIP file.
                type: string
              file:
                description: "If you are calling a local KJB file, this is the filename,\
                  \ including the path if it is not in the local directory"
                type: string
              image:
                description: the Docker image default is rossonet/kettle-runner:latest
                type: string
              job:
                description: The name of the job (as it appears in the repository)
                  to launch
                type: string
              level:
                description: "The logging level (Basic, Detailed, Debug, Rowlevel,\
                  \ Error, Nothing)"
                type: string
              listdir:
                description: Lists the sub-directories within the specified repository
                  directory
                type: string
              listjob:
                description: Lists the jobs in the specified repository directory
                type: string
              listrep:
                description: Lists the available repositories
                type: string
              logfile:
                description: A local filename to write log output to
                type: string
              norep:
                description: "Prevents Kitchen from logging into a repository. If\
                  \ you have set the KETTLE_REPOSITORY, KETTLE_USER, and KETTLE_PASSWORD\
                  \ environment variables, then this option will enable you to prevent\
                  \ Kitchen from logging into the specified repository, assuming you\
                  \ would like to execute a local KTR file instead."
                type: string
              param:
                description: Set a named parameter in a name=value format. For
                items:
                  type: string
                type: array
              pass:
                description: Repository password
                type: string
              rep:
                description: "Enterprise or database repository name, if you are using\
                  \ one"
                type: string
              schedule:
                description: schedule notation like crontab
                type: string
              user:
                description: Repository username
                type: string
              version:
                description: "Shows the version, revision, and build date"
                type: string
              xmlRepository:
                description: XML with repositories configuration
                type: string
            required:
            - dir
            - schedule
            type: object
          status:
            properties:
              messages:
                items:
                  type: string
                type: array
              returnCode:
                type: integer
              returnCodeDescription:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: cronkettletransformations.kettle.rossonet.net
spec:
  group: kettle.rossonet.net
  names:
    kind: CronKettleTransformation
    plural: cronkettletransformations
    shortNames:
    - kct
    singular: cronkettletransformation
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              dir:
                description: "The repository directory that contains the transformation,\
                  \ including the leading slash"
                type: string
              exprep:
                description: Exports all repository objects to one XML file
                type: string
              file:
                description: "If you are calling a local KTR file, this is the filename,\
                  \ including the path if it is not in the local directory"
                type: string
              image:
                description: the Docker image default is rossonet/kettle-runner:latest
                type: string
              level:
                description: "The logging level (Basic, Detailed, Debug, Rowlevel,\
                  \ Error, Nothing)"
                type: string
              listdir:
                description: Lists the directories in the specified repository
                type: string
              listrep:
                description: Lists the available repositories
                type: string
              listtrans:
                description: Lists the transformations in the specified repository
                  directory
                type: string
              logfile:
                description: A local filename to write log output to
                type: string
              norep:
                description: "Prevents Pan from logging into a repository. If you\
                  \ have set the KETTLE_REPOSITORY, KETTLE_USER, and KETTLE_PASSWORD\
                  \ environment variables, then this option will enable you to prevent\
                  \ Pan from logging into the specified repository, assuming you would\
                  \ like to execute a local KTR file instead."
                type: string
              param:
                description: "Set a named parameter in a name=value format. For example:\
                  \ -param:FOO=bar"
                items:
                  type: string
                type: array
              pass:
                description: Repository password
                type: string
              rep:
                description: "Enterprise or database repository name, if you are using\
                  \ one"
                type: string
              safemode:
                description: "Runs in safe mode, which enables extra checking"
                type: string
              schedule:
                description: schedule notation like crontab
                type: string
              trans:
                description: The name of the transformation (as it appears in the
                  repository) to launch
                type: string
              user:
                description: Repository username
                type: string
              version:
                description: "Shows the version, revision, and build date"
                type: string
              xmlRepository:
                description: XML with repositories configuration
                type: string
            required:
            - dir
            - schedule
            type: object
          status:
            properties:
              messages:
                items:
                  type: string
                type: array
              returnCode:
                type: integer
              returnCodeDescription:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: kettleides.kettle.rossonet.net
spec:
  group: kettle.rossonet.net
  names:
    kind: KettleIde
    plural: kettleides
    shortNames:
    - ki
    singular: kettleide
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              host:
                description: Ingress hostname
                type: string
              image:
                description: Docker image
                type: string
              path:
                description: Ingress path
                type: string
              servicePort:
                description: Ingress service port
                type: integer
              xmlRepository:
                description: XML with repositories configuration
                type: string
            type: object
          status:
            properties:
              messages:
                items:
                  type: string
                type: array
              returnCode:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: kettlejobs.kettle.rossonet.net
spec:
  group: kettle.rossonet.net
  names:
    kind: KettleJob
    plural: kettlejobs
    shortNames:
    - kj
    singular: kettlejob
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              dir:
                description: "The repository directory that contains the job, including\
                  \ the leading slash"
                type: string
              export:
                description: Exports all linked resources of the specified job. The
                  argument is the name of a ZIP file.
                type: string
              file:
                description: "If you are calling a local KJB file, this is the filename,\
                  \ including the path if it is not in the local directory"
                type: string
              image:
                description: the Docker image default is rossonet/kettle-runner:latest
                type: string
              job:
                description: The name of the job (as it appears in the repository)
                  to launch
                type: string
              level:
                description: "The logging level (Basic, Detailed, Debug, Rowlevel,\
                  \ Error, Nothing)"
                type: string
              listdir:
                description: Lists the sub-directories within the specified repository
                  directory
                type: string
              listjob:
                description: Lists the jobs in the specified repository directory
                type: string
              listrep:
                description: Lists the available repositories
                type: string
              logfile:
                description: A local filename to write log output to
                type: string
              norep:
                description: "Prevents Kitchen from logging into a repository. If\
                  \ you have set the KETTLE_REPOSITORY, KETTLE_USER, and KETTLE_PASSWORD\
                  \ environment variables, then this option will enable you to prevent\
                  \ Kitchen from logging into the specified repository, assuming you\
                  \ would like to execute a local KTR file instead."
                type: string
              param:
                description: Set a named parameter in a name=value format. For
                items:
                  type: string
                type: array
              pass:
                description: Repository password
                type: string
              rep:
                description: "Enterprise or database repository name, if you are using\
                  \ one"
                type: string
              user:
                description: Repository username
                type: string
              version:
                description: "Shows the version, revision, and build date"
                type: string
              xmlRepository:
                description: XML with repositories configuration
                type: string
            required:
            - dir
            type: object
          status:
            properties:
              messages:
                items:
                  type: string
                type: array
              returnCode:
                type: integer
              returnCodeDescription:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: kettlerepositories.kettle.rossonet.net
spec:
  group: kettle.rossonet.net
  names:
    kind: KettleRepository
    plural: kettlerepositories
    shortNames:
    - kr
    singular: kettlerepository
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              databaseName:
                description: Database name
                type: string
              image:
                description: the Docker image default is rossonet/kettle-repository:latest
                type: string
              password:
                description: Database password
                type: string
              replicas:
                description: Database replicas
                type: integer
              repositoryPassword:
                description: Repository password
                type: string
              repositorySshCertificate:
                description: Repository ssh certificate
                type: string
              repositoryUrl:
                description: "repository url to get the data. Should start with the\
                  \ protocol 'git-ssh://', 'git-http://', 'git-https://', 's3://',\
                  \ 'ftp://', 'http://', 'https://' or 'file://'"
                type: string
              repositoryUsername:
                description: Repository username
                type: string
              username:
                description: Database username
                type: string
            type: object
          status:
            properties:
              messages:
                items:
                  type: string
                type: array
              returnCode:
                type: string
              totalJobs:
                type: integer
              totalTransformations:
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: kettletransformations.kettle.rossonet.net
spec:
  group: kettle.rossonet.net
  names:
    kind: KettleTransformation
    plural: kettletransformations
    shortNames:
    - kt
    singular: kettletransformation
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              dir:
                description: "The repository directory that contains the transformation,\
                  \ including the leading slash"
                type: string
              exprep:
                description: Exports all repository objects to one XML file
                type: string
              file:
                description: "If you are calling a local KTR file, this is the filename,\
                  \ including the path if it is not in the local directory"
                type: string
              image:
                description: the Docker image default is rossonet/kettle-runner:latest
                type: string
              level:
                description: "The logging level (Basic, Detailed, Debug, Rowlevel,\
                  \ Error, Nothing)"
                type: string
              listdir:
                description: Lists the directories in the specified repository
                type: string
              listrep:
                description: Lists the available repositories
                type: string
              listtrans:
                description: Lists the transformations in the specified repository
                  directory
                type: string
              logfile:
                description: A local filename to write log output to
                type: string
              norep:
                description: "Prevents Pan from logging into a repository. If you\
                  \ have set the KETTLE_REPOSITORY, KETTLE_USER, and KETTLE_PASSWORD\
                  \ environment variables, then this option will enable you to prevent\
                  \ Pan from logging into the specified repository, assuming you would\
                  \ like to execute a local KTR file instead."
                type: string
              param:
                description: "Set a named parameter in a name=value format. For example:\
                  \ -param:FOO=bar"
                items:
                  type: string
                type: array
              pass:
                description: Repository password
                type: string
              rep:
                description: "Enterprise or database repository name, if you are using\
                  \ one"
                type: string
              safemode:
                description: "Runs in safe mode, which enables extra checking"
                type: string
              trans:
                description: The name of the transformation (as it appears in the
                  repository) to launch
                type: string
              user:
                description: Repository username
                type: string
              version:
                description: "Shows the version, revision, and build date"
                type: string
              xmlRepository:
                description: XML with repositories configuration
                type: string
            type: object
          status:
            properties:
              messages:
                items:
                  type: string
                type: array
              returnCode:
                type: integer
              returnCodeDescription:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
